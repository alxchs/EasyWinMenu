<theming:ModernWindow x:Class="EasyWinMenu.App.Settings.LaunchItemEditWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:theming="clr-namespace:EasyWinMenu.App.Theming"
                      xmlns:loc="clr-namespace:EasyWinMenu.App.Localization"
                      Title="{loc:Loc item.title}"
                      Width="420" Height="450"
                      WindowStartupLocation="CenterOwner"
                      ResizeMode="NoResize"
                      ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{loc:Loc item.name}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox x:Name="NameBox" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="{loc:Loc item.type}" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox x:Name="TypeBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="{loc:Loc item.target}" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox x:Name="TargetBox" Grid.Row="2" Grid.Column="1" Margin="0,0,8,8" />
        <Button Content="{loc:Loc item.browse}" Grid.Row="2" Grid.Column="2" Margin="0,0,0,8" Click="OnBrowseClick" />

        <TextBlock Text="{loc:Loc item.arguments}" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox x:Name="ArgumentsBox" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="{loc:Loc item.workingDirectory}" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox x:Name="WorkingDirectoryBox" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="{loc:Loc item.executionMode}" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox x:Name="ExecutionModeBox" Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" />

        <TextBlock Text="{loc:Loc item.customIcon}" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox x:Name="IconOverrideBox" Grid.Row="6" Grid.Column="1" Margin="0,0,8,8" />
        <Button Content="{loc:Loc item.browse}" Grid.Row="6" Grid.Column="2" Margin="0,0,0,8" Click="OnBrowseIconClick" />

        <TextBlock Text="{loc:Loc item.pinToDesktop}" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,8" />
        <CheckBox x:Name="DesktopPinnedBox" Grid.Row="7" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,8" />

        <StackPanel Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{loc:Loc common.ok}" Width="80" IsDefault="True" Click="OnOkClick" Margin="0,0,8,0" />
            <Button Content="{loc:Loc common.cancel}" Width="80" IsCancel="True" />
        </StackPanel>
    </Grid>
</theming:ModernWindow>
